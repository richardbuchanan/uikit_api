{#
/**
 * @file
 * Theme override to display a details element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the details element.
 * - errors: (optional) Any errors for this details element, may not be set.
 * - title: (optional) The title of the element, may not be set.
 * - description: (optional) The description of the element, may not be set.
 * - children: (optional) The children of the element, may not be set.
 * - value: (optional) The value of the element, may not be set.
 *
 * @see template_preprocess_details()
 * @see uikit_preprocess_details().
 * @see uikit_admin_preprocess_details()
 *
 * @ingroup uikit_admin_themeable
 */
#}
{{ attach_library('uikit/uikit_accordion') }}
<div{{ accordion_attributes }}>
  {%- if title -%}
    <h3{{ accordion_title_attributes }}>{{ title }}</h3>
  {%- endif -%}

  <div{{ accordion_content_attributes }}>
    {% if errors %}
      <div{{ error_classes }}>
        {{ errors }}
      </div>
    {% endif %}

    {% if description %}
      <div{{ description_attributes }}>
        {{ description }}
      </div>
    {% endif %}

    {{ children }}
    {{ value }}
  </div>
</div>
